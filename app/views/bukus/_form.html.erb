<%= form_with(model: buku, local: true) do |form| %>
  <% if buku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(buku.errors.count, "error") %> prohibited this buku from being saved:</h2>

      <ul>
      <% buku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
